import React, { useState, useEffect } from "react";
import { Button, Form, Upload } from "antd";
import { Input, Image } from "antd";
import "./EditForm.css";
import { useLocation } from "react-router-dom";
import { UploadOutlined } from "@ant-design/icons";
import { getSingleProjects } from "../../APIs/UserApi";


function EditForm() {
  const location = useLocation();
  const { project } = location.state;
  console.log("edit project data edit form ..", project);
  let id = project._id;

  const [projectName, setProjectName] = useState()
  console.log("nam..", projectName)

  const onFinish = (values) => {
    console.log('Success:', values);
  };




  const [form] = Form.useForm();


  const [data, setData] = useState()

  useEffect(() => {
    loadData();
  }, []);


  const loadData = async () => {
    await getSingleProjects(id).then((response) => {
      console.log("single react projects..", response.data.projects);
      form.setFieldsValue({
        sample: response.data.projects.projectName
      })

    });
  };

 

  





  return (
    <div>
      <div className="EditFormWrapper">
        <Form
          form={form}
          onFinish={onFinish}
        >

          <Form.Item name="sample">
            <Input />
          </Form.Item>




          <Form.Item
            wrapperCol={{
              offset: 8,
              span: 16,
            }}
          >

            <Button type="primary" htmlType="submit">
              Submit
            </Button>
          </Form.Item>

        </Form>
      </div>
    </div >
  );
}

export default EditForm;
